
function countToN(n){
    //Use a while loop to count to ten, using console.log to print what number you are on
    // (YOUR CODE HERE)
    let x = 1 // Starting at 1
    while (x<1) { // while x is 1 log to console x
        console.log("counting to", x); //
        x++;// while logging to conole at 1 to x
    }
    
    for (let x=1; x<4; x++) 
    console.log("Counting to N",x);


while (x<5) { console.log ("count to", x);
x++
}
 for (let x =5; x < 6; x++);
 console.log("Count to N", x);
}

countToN(3);

countToN(5);